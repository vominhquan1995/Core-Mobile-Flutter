import 'package:Midas/core/translate/localization.dart';
import 'package:flutter/material.dart';

class DataJsonNotFound extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(tr('core_23')),
      ),
    );
  }
}
